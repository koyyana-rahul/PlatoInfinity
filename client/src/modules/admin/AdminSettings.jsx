import PlaceholderPage from "../../components/ui/PlaceholderPage";

export default function AdminSettings() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Settings page will live here (brand settings, user profile, billing settings, etc.)."
    />
  );
}
